class Constant {
  static const devMode = true;
  static const userInfoCollection = 'userInfo_collection';
  static const activityCollection = 'activities';
  static const doctorCollection = 'doctors';
  static const logsCollection = 'logs_collection';
}

enum ArgKey {
  user,
  users,
  userInfo,
  activityList,
  friendActivityList,
  activity,
  friend,
  doctorList,
  doctor,
  covidData,
  logsList,
  completed,
  city,
}
